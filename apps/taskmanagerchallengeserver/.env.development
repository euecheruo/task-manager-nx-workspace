# ----------------------------------------------
# DEVELOPMENT ENVIRONMENT CONFIGURATION
# Used when NODE_ENV is explicitly set to 'development' or is unset (default)
# ----------------------------------------------

# Application Settings
NODE_ENV=development
APP_PORT=3000

# ----------------------------------------------
# PostgreSQL Database Configuration
# NOTE: In a local Nx environment, this often points to a local Docker container or service
# The 'synchronize' setting is typically TRUE for dev/testing tables auto-generation
# ----------------------------------------------
POSTGRES_HOST='localhost'
POSTGRES_PORT=5432
POSTGRES_USER='postgres'
POSTGRES_PASSWORD='wAGMpE8)bJDm4ioFi#U'
POSTGRES_DB='task-manager-challenge-db'
# TYPEORM_SYNCHRONIZE will be inferred as TRUE by config factory when NODE_ENV=development

# ----------------------------------------------
# JWT Configuration (Authentication)
# Use short expiration times for frequent testing and rotation
# ----------------------------------------------
JWT_ACCESS_SECRET='988b40b0dca30f1151db48e3437ea16a363937a0ef287e199ef72a7d21b3e51d' # MUST be 256 bits (32 bytes) or more in production
JWT_REFRESH_SECRET='95a2bf27feca13def5dc4570c67a0e2627b0e8ffb47edea65f1888c5757d1736'
JWT_ACCESS_EXPIRATION=15m
JWT_REFRESH_EXPIRATION=7d

# ----------------------------------------------
# Security/Hashing Configuration
# ----------------------------------------------
# BCRYPT_SALT_ROUNDS=10 # Example if you needed a dedicated salt round variable

FRONTEND_URL=http://localhost:4200